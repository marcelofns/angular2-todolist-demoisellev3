
export class Todo{
  id: number;
  nome: string;
  status: string;
  datafim: string;
  adiada: boolean;
  usuario: string;
  constructor(
    id?: number,
    nome?: string,
    status?: string,
    datafim?: string,
    adiada?: boolean,
    usuario?: string

  ) {  }

}